import { StyleSheet } from "react-native";

export const categoryCardStyle = StyleSheet.create({
    container: {
        height: 106,
        padding: 17,
        width: "48%",
        borderRadius: 8,
        overflow: "hidden",
    },
    iconWrapper: {
        right: 0,
        bottom: -2,
        position: "absolute",
    },
});
