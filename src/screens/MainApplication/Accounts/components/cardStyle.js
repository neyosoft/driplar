import { StyleSheet } from "react-native";

export const cardStyles = StyleSheet.create({
    container: {
        height: 106,
        padding: 15,
        width: "48%",
        borderRadius: 8,
    },
    iconWrapper: {
        right: 8,
        bottom: 8,
        position: "absolute",
    },
});
